# Example: docker-engine

```console
$ pandion validate -f cluster.yaml
$ pandion up -f cluster.yaml --id docker-engine
# ... streams the run output ...
$ pandion down --id docker-engine
```

See [cluster.yaml](cluster.yaml) for the topology and the
[examples index](../README.md) for the other examples.
