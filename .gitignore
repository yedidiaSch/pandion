# build output
/pandion
/cmd/pandion/pandion
*.exe

# local state written by the demo
.pandion/

# editor / OS
.DS_Store
*.swp
/bin/

# goreleaser build output
/dist/

# internal strategy notes (do not publish)
/docs/monetization-strategy.md

# secrets — API tokens for real-cloud e2e (NEVER commit)
.env
.env.*
!.env.example
relayprobe-*/
relayleprobe-*/
/pandion-relay
relayrecprobe-*/
